# REDUX TOOLKIT

-install redux toolkit (npm i @reduxjs/toolkit npm i react-redux)-- these two libraries have to be installed
-Build our own store
-we wll connect our store to app
-create a slice (name , initialState, reducers -- this reducer is an object gets two parameters like (state, action))
-dispatch
-selector
